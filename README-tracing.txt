Asterisk contains lists of tracepoints. That are available.

# LD_PRELOAD=liblttng-ust-fork.so asterisk

You can then check what the currently available tracepoints are with the
following command:

# lttng list -u

